const fn = fn => []

export const [JobDetailsProvider, useJobDetails] = fn(() => {
  const { ...query } = {}
})

export const [CandidatesProvider, useCandidates] = fn(() => {
  const { ...query } = {}
})
